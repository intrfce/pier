valkey:
    image: 'valkey/valkey:alpine'
    ports:
        - '${FORWARD_VALKEY_PORT:-6379}:6379'
    volumes:
        - 'pier-valkey:/data'
    networks:
        - pier
    healthcheck:
        test: ["CMD", "valkey-cli", "ping"]
        retries: 3
        timeout: 5s
